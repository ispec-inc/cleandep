rules:
  - source: a
    illegal_dependencies:
      - fmt
      - math/rand
      - net/*
  - source: a/b
    illegal_dependencies:
      - log
      - math/rand
      - net/*
  - source: "**/c/**/*"
    illegal_dependencies:
      - log
      - math/rand
      - net/*
